## MODIFIED Requirements

### Requirement: Front door slide layout includes registration banner
The front door slide SHALL render a `RegistrationBanner` component in the vertical flow between the "presenterar" heading and the door picture.

#### Scenario: Banner appears between heading and door
- **WHEN** the front door slide is rendered
- **THEN** the `RegistrationBanner` component is visible between the heading text and the door image
